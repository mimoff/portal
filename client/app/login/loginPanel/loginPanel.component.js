import template from './loginPanel.html';
import controller from './loginPanel.controller';
import './loginPanel.styl';

let loginPanelComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default loginPanelComponent;
